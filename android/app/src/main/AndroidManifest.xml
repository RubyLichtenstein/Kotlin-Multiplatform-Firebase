<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rubylichtenstein.outconnect">

    <application
        android:name=".app.MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_android_black_24dp"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_android_black_24dp"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <service
            android:name=".notifications.service.MyFirebaseMessagingService"
            android:exported="false"
            android:permission="">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_android_black_24dp" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/primaryColor" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_channel" />

        <activity android:name=".PostsActivity"></activity>
    </application>

</manifest>